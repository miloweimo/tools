# 站点标题与 favicon

**日期**：2026-03-25  
**类型**：配置 / 资源

## 改动说明

- [`index.html`](../../index.html)：`lang="zh-CN"`，`<title>` 改为「小工具集」，增加简短 `description`；`favicon` 改为 [`public/favicon.svg`](../../public/favicon.svg)（相对路径 `./favicon.svg`，与 `base: '/tools/'` 部署一致）。
- 新增 SVG 图标：圆角深色底 + 四格绿色块，示意「工具集合」。
