---
description: 实质改动后归档到 ai/changelog 或 ai/guides
alwaysApply: true
---

# 改动归档（changelog / guides）

在完成**有用户价值的改动**后（新功能、行为变更、路由/工具注册、依赖或构建调整、对外文档等），将修改内容**落盘记录**，二选一或同时：

1. **`ai/changelog/`**：面向「这次做了什么」的流水/发布说明。新建 `YYYY-MM-DD-主题简述.md`，风格对齐现有条目（标题、日期、类型、**改动说明** 用列表 + 相对链接指向改动的源文件）。同一日多次可合并进同一文件或分文件，保持可读即可。
2. **`ai/guides/`**：面向**长期有效的**上下文、流程、里程碑任务（例如更新 `dev-tasks.md` 勾选、在 `project-context.md` 补充部署/结构约定）。

**可省略**：纯错别字、仅格式化、无行为影响的极小补丁；若仍希望留痕，可在当日 changelog 中加一行即可。

**注意**：路径为 `ai/guides`（复数），不要写成 `ai/guide`。
